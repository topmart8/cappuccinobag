import { Link, PetBreadcrumb, PetShell } from "../pet-travel-components";
import { petArticles } from "../pet-travel-articles";
import { petGuidesUrl, petSiteUrl } from "../pet-travel-data";

export const metadata = { title: "Pet Travel Bag OEM Buyer Guides | Cappuccino Bag", description: "Twelve practical guides for pet carrier, organizer, walking accessory, sampling, materials, quality control and private-label sourcing projects.", alternates: { canonical: `${petSiteUrl}${petGuidesUrl}` }, robots: { index: true, follow: true } };
export default function PetTravelGuidesPage() { return <PetShell><main className="pet-page"><PetBreadcrumb items={[{ name: "Pet Travel Bags", href: "/pet-travel-bags" }, { name: "Buyer Guides" }]} /><section className="pet-simple-hero"><p className="eyebrow">12 procurement resources</p><h1>Pet Travel Bag OEM Buyer Guides</h1><p className="pet-lead">Practical guidance for brands, retailers, distributors and product teams planning custom pet carriers, organizers, backpacks and walking accessories.</p></section><section className="pet-section"><div className="pet-guide-grid">{petArticles.map((article, index) => <article key={article.slug}><p className="eyebrow">Guide {String(index + 1).padStart(2, "0")}</p><h2><Link href={`${petGuidesUrl}/${article.slug}`}>{article.title}</Link></h2><p>{article.metaDescription}</p><Link className="pet-text-link" href={`${petGuidesUrl}/${article.slug}`}>Read buyer guide</Link></article>)}</div></section></main></PetShell>; }
